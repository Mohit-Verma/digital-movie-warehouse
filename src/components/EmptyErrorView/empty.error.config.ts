enum EMODE {
    ERROR = 'ERROR',
    EMPTY = 'EMPTY'
}

const RESOURCE_PATH = {
    EMPTY_IMAGE: '/resources/empty_image.png',
    ERROR_IMAGE: '/resources/error_image.png'
};

const LABELS = {
    RETRY: 'Retry'
};

export {
    EMODE,

    RESOURCE_PATH,
    LABELS
};
